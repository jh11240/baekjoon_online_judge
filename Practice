using namespace std;

int binarySearch(int target,int n)
{
    int low=0;
    int high= n-1;

    while(low<=high)
    {
        int mid = (low + high)/2;

        if(mid == target) return mid;

        else if(mid>target) low =mid+1;
        else
            high = mid -1;
    }
    return -1;
}